name: okws_client
description: A simplified WebSocket client with robust reconnection handling, supporting custom headers, ping interval, and backoff strategies.
version: 1.0.0
environment:
  sdk: ^3.0.0

dependencies:
  
dev_dependencies:
  test: ^1.28.0
